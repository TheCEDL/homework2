pro5:
I failed to handle the spicy problem although I used the following code:
sudo apt-get install python-scipy
pip install scipy
import scipy

pro6:
I guess when run the whole process, 'advantages' may be calculated to some format inconsistent with the codes using it below,
so it may produce bugs if we do not normalize it first.
While '+ 1e-8' is to avoid the denominator turning to zero.
